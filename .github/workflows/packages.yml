name: MSYS2 PKGBUILD CI

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '**.md'
      - '.github/**'
      - '.vscode/**'
      - '.devcontainer/**'
  pull_request:

jobs:
  detect-directory-changes:
    name: "Detect Directory Changes"
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.detect.outputs.changed }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Detect Directory Changes
        id: detect
        uses: tchupp/actions-detect-directory-changes@v1
        with:
          included-paths: "!./.github/**"
  build-pkg:
    runs-on: windows-11-arm
    needs: [detect-directory-changes]
    strategy:
      fail-fast: false
      matrix:
        project: ${{ fromJSON(needs.detect-directory-changes.outputs.changed) }}
    steps:
    - uses: actions/checkout@v5
    - name: Setup MSYS2
      uses: msys2/setup-msys2@v2
      with:
        msystem: CLANGARM64
        update: true
        install:
            git
            make
            patch
            patchutils
        pacboy:
            toolchain:a
            gcc-compat:a
            cmake:a
            diffutils:a
    - name: makepkg-mingw
      working-directory: ./${{ matrix.project }}
      shell: msys2 {0}
      run: |
        # makepkg-mingw on project directory: ${{ matrix.project }}
        makepkg-mingw --cleanbuild --force --noconfirm --syncdeps --skippgpcheck
    - name: '📤 Upload artifact: package'
      uses: actions/upload-artifact@v6
      with:
        name: ${{ matrix.project }}-aarch64
        path: ./${{ matrix.project }}/*.zst
  release:
    name: Release
    needs: [build-pkg]
    # if: github.event_name != 'pull_request' && startswith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Generate dated tag name
      id: generate_tag
      run: |
        # Format date as YYYY-MM-DD-HHMMSS (e.g., release-2025-01-13-165900)
        TAG_NAME="release-$(date +'%Y-%m-%d-%H%M%S')"
        echo "Generated tag name: $TAG_NAME"
        # Set the tag name as an output variable for subsequent steps
        echo "new_tag=$TAG_NAME" >> $GITHUB_ENV
    - name: Create and push the new tag
      uses: rickstaa/action-create-tag@v1
      with:
        tag: ${{ env.new_tag }}
        message: "Automated release tag: ${{ env.new_tag }}"
        github_token: ${{ secrets.GITHUB_TOKEN }}
    - name: Prepare release directory
      run: |
        mkdir -p release
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        path: release
    - name: Zip directories
      run: |
        cd release
        for f in *; do
          if [ -d "$f" ]; then
            cd $f
            echo "Compressing $f"
            zip -r $f.zip *
            mv $f.zip ../
            cd ..
          fi
        done
    - name: Create release
      run: |
        gh release create ${{ github.ref_name }} ./release/*.zip \
          --title "${{ env.NAME_UPPER }} ${{ github.ref_name }}" \
          --tag {{ env.new_tag }}
          --draft \
          --generate-notes
      env:
        GITHUB_TOKEN: ${{ github.TOKEN }}
